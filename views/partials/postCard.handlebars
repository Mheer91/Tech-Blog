<div class="row">
    <div class="">
      <div class="card blue-grey darken-1">
        <div class="card-content white-text">
          <span class="card-title">{{post_title}}</span>
          <p>{{post_body}}</p>
        </div>
        <div class="card-action">
          <a href="#">Comments</a>
          <p class="right">Posted By: {{user.username}} on {{creation_date}}</p>
        </div>
      </div>
    </div>
</div>